/**
 * Generated by orval v8.23.0 🍺
 * Do not edit manually.
 * Api
 * API specification
 * OpenAPI spec version: 0.1.0
 */
export interface HealthStatus {
  status: string;
}

export interface AuthUser {
  id: string;
  /** @nullable */
  email: string | null;
  /** @nullable */
  firstName: string | null;
  /** @nullable */
  lastName: string | null;
  /** @nullable */
  profileImageUrl: string | null;
}

export interface AuthUserEnvelope {
  user: AuthUser | null;
}

/**
 * Active authentication strategy, selected server-side via the AUTH_MODE env var.
 */
export type AuthConfigMode = typeof AuthConfigMode[keyof typeof AuthConfigMode];


export const AuthConfigMode = {
  replit: 'replit',
  local: 'local',
} as const;

export interface AuthConfig {
  /** Active authentication strategy, selected server-side via the AUTH_MODE env var. */
  mode: AuthConfigMode;
}

export interface LocalRegisterInput {
  email: string;
  /** @minLength 8 */
  password: string;
  firstName?: string;
  lastName?: string;
}

export interface LocalLoginInput {
  email: string;
  /** @minLength 1 */
  password: string;
}

export interface MobileTokenExchangeRequest {
  /** @minLength 1 */
  code: string;
  /** @minLength 1 */
  code_verifier: string;
  /** @minLength 1 */
  redirect_uri: string;
  /** @minLength 1 */
  state: string;
  /** @minLength 1 */
  nonce?: string;
}

export interface MobileTokenExchangeSuccess {
  token: string;
}

export const LogoutSuccessValue = {
  success: true,
} as const;
export type LogoutSuccess = typeof LogoutSuccessValue;

export interface ErrorEnvelope {
  error: string;
}

export type ProviderName = typeof ProviderName[keyof typeof ProviderName];


export const ProviderName = {
  'cloudflare-workers-ai': 'cloudflare-workers-ai',
  'cloudflare-ai-gateway': 'cloudflare-ai-gateway',
  openrouter: 'openrouter',
  openai: 'openai',
  anthropic: 'anthropic',
  'google-ai-studio': 'google-ai-studio',
  groq: 'groq',
  mistral: 'mistral',
  deepseek: 'deepseek',
  xai: 'xai',
  custom: 'custom',
} as const;

export interface ProviderField {
  /** Credential key stored server-side (e.g. `apiToken`). */
  key: string;
  /** Human label for the settings UI. */
  label: string;
  /** Whether this field is a secret (rendered as a masked password input). */
  secret: boolean;
  required: boolean;
  placeholder?: string;
  /**
     * Masked preview of the stored value (e.g. `••••4f2a`), null when not set.
     * @nullable
     */
  maskedPreview?: string | null;
}

export type ProviderStatus = typeof ProviderStatus[keyof typeof ProviderStatus];


export const ProviderStatus = {
  untested: 'untested',
  ok: 'ok',
  error: 'error',
} as const;

export interface Provider {
  name: ProviderName;
  displayName: string;
  /** One-line explanation shown on the provider card. */
  description: string;
  /**
     * Link to where the user can obtain credentials.
     * @nullable
     */
  docsUrl?: string | null;
  fields: ProviderField[];
  /** Whether all required credentials are stored. */
  configured: boolean;
  isDefault: boolean;
  status: ProviderStatus;
  /**
     * Exact message from the last connection test.
     * @nullable
     */
  statusMessage?: string | null;
  /** @nullable */
  lastTestedAt?: string | null;
}

/**
 * Map of credential key to plaintext value. Only fields present are updated.
 */
export type ProviderCredentialsInputCredentials = {[key: string]: string};

export interface ProviderCredentialsInput {
  /** Map of credential key to plaintext value. Only fields present are updated. */
  credentials: ProviderCredentialsInputCredentials;
  /** When true, mark this provider as the default. */
  isDefault?: boolean;
}

export interface ProviderTestResult {
  ok: boolean;
  /** Human-readable result — "OK" or the exact provider error. */
  message: string;
  /** @nullable */
  latencyMs?: number | null;
}

/**
 * Opaque session token — `Bearer <sid>`.
 */
export type AuthorizationSessionHeaderParameter = string;

export type BeginBrowserLoginParams = {
/**
 * Relative path to redirect to after login (must start with `/`). Defaults to `/`.
 */
returnTo?: string;
};

export type HandleBrowserLoginCallbackParams = {
code?: string;
state?: string;
iss?: string;
};

export type LogoutBrowserSessionParams = {
returnTo?: string;
};

